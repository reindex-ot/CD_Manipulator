# MICROSOFT FOUNDATION CLASS ライブラリ：Hermit2 プロジェクトの概要

アプリケーションウィザードが作成した **Hermit2** には、Microsoft Foundation Classes (MFC) の基本的な使い方が示されています。アプリケーション作成のひな型としてお使いください。

このドキュメントには、Hermit2 アプリケーションを構成している各ファイルの概要説明が含まれています。

---

## 1. プロジェクトおよびメインアプリケーション

### Hermit2.vcproj
アプリケーションウィザードで生成された VC++ プロジェクトのメインプロジェクトファイルです。ファイルが生成された Visual C++ のバージョン情報や、ウィザードで選択したプラットフォーム、構成、およびプロジェクト機能に関する情報が含まれています。

### Hermit2.h
アプリケーションの中心となるヘッダーファイルです。`Resource.h` をはじめとするプロジェクト固有のヘッダーをインクルードし、`CHermit2App` アプリケーションクラスの宣言を行います。

### Hermit2.cpp
`CHermit2App` アプリケーションクラスの実装を含む、アプリケーションの中心となるソースファイルです。

---

## 2. リソースファイル

### Hermit2.rc
プログラムが使用する Microsoft Windows のリソース（アイコン、ビットマップ、カーソルなど）を列挙するファイルです。これらの一部は `res` サブディレクトリに保存されています。このファイルは Microsoft Visual C++ で直接編集可能です（プロジェクトリソース言語：日本語 `1041`）。

### res\Hermit2.ico
アプリケーションのメインアイコンとして使用されるアイコンファイルです。リソースファイル `Hermit2.rc` によってインクルードされます。

### res\Hermit2.rc2
Microsoft Visual C++ のリソースエディタによって直接編集されないリソースを記述するファイルです。手動で管理したいリソースはすべてこのファイルに記述してください。

### Resource.h
新規リソース ID を定義する標準ヘッダーファイルです。Microsoft Visual C++ が自動的にこのファイルの読み込みと更新を行います。

---

## 3. UI・ダイアログクラス

### Hermit2Dlg.h, Hermit2Dlg.cpp
メインダイアログの動作を定義する `CHermit2Dlg` クラスの宣言と実装です。ダイアログのレイアウト（テンプレート）は、Visual C++ で編集可能な `Hermit2.rc` に含まれています。

---

## 4. プリコンパイル済みヘッダー

### StdAfx.h, StdAfx.cpp
プリコンパイル済みヘッダーファイル（PCH）である `Hermit2.pch` や、コンパイル済みの型ファイル `StdAfx.obj` をビルドするために使用されるファイルです。

---

## 5. 開発時の注意点・実装メモ

* **TODO コメント**
  アプリケーションウィザードは、追加の実装やカスタマイズが必要なソースコード部分に `"TODO:"` で始まるコメントを挿入しています。
* **MFCのローカライズ（共有DLL使用時）**
  共有 DLL 内で MFC を使用する場合や、OS の言語と異なるリソースを使用する場合は、Visual C++ のメディアから対応するローカライズ版 DLL（日本語の場合は `MFC70JPN.DLL` など）を `system` または `system32` ディレクトリにコピーし、ファイル名を `MFCLOC.DLL` に変更する必要があります。この手順をスキップすると、一部の共通 UI 要素が OS の既定言語のまま表示されることがあります。